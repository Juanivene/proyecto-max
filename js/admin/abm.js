import { Pelicula } from "./pelicula.js";

export const agregarPelicula =
  (titulo,
  genero,
  descripcion,
  fotoCaratula,
  fotoPortada,
  linkTrailer,
  tipo,
  estaPublicada)=>{
    const pelicula = new Pelicula()
  };
